<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice {{invoiceNumber}}</title>
    <style>
         * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 12px;
            line-height: 1.4;
            color: #333;
            background: white;
        }
        
        .invoice-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 15px;
        }
        
        .header {
            text-align: center;
            border: 2px solid #2c3e50;
            padding: 15px;
        }
        
        .invoice-title {
            font-size: 28px;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 10px;
        }
        
        .invoice-number {
            font-size: 14px;
            color: #34495e;
        }
        
        .invoice-date {
            font-size: 14px;
            color: #34495e;
        }
        
        .invoice-type-badge {
            position: absolute;
            top: 20px;
            right: 20px;
            background: #3498db;
            color: white;
            padding: 8px 15px;
            border-radius: 4px;
            font-size: 10px;
            font-weight: bold;
        }
        
        .period-info {
            margin: 10px 0;
            font-weight: bold;
        }
        
        .details-section {
            display: flex;
            gap: 20px;
            margin-bottom: 10px;
        }
        
        .vendor-details, .plant-details {
            flex: 1;
            border: 1px solid #bdc3c7;
            padding: 10px;
        }
        
        .section-title {
            font-weight: bold;
            font-size: 14px;
            margin-bottom: 10px;
            color: #2c3e50;
        }
        
        .detail-row {
            margin-bottom: 5px;
            font-size: 11px;
        }
        
        .material-breakdown {
            margin-bottom: 10px;
        }
        
        .breakdown-title {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 5px;
            color: #2c3e50;
        }
        
        .breakdown-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 10px;
        }
        
        .breakdown-table th {
            background: #ecf0f1;
            padding: 8px;
            text-align: left;
            font-weight: bold;
            font-size: 10px;
            border: 1px solid #bdc3c7;
        }
        
        .breakdown-table td {
            padding: 8px;
            border: 1px solid #bdc3c7;
            font-size: 10px;
        }
        
        .summary-section {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .summary-box {
            flex: 1;
            border: 2px solid #3498db;
            padding: 10px;
        }
        
        .gst-box {
            flex: 1;
            border: 2px solid #8e44ad;
            padding: 10px;
        }
        
        .gst-title {
            font-weight: bold;
            font-size: 12px;
            margin-bottom: 10px;
            color: #8e44ad;
        }
        
        .gst-row {
            margin-bottom: 5px;
            font-size: 11px;
        }
        
        .final-amount-box {
            background: #27ae60;
            color: white;
            padding: 10px;
            text-align: center;
            border-radius: 4px;
        }
        
        .final-amount-label {
            font-size: 14px;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .final-amount-value {
            font-size: 20px;
            font-weight: bold;
        }
        
        .signature-section {
            margin-top: 40px;
            border: 1px solid #bdc3c7;
            padding: 10px;
        }
        
        .signature-title {
            font-weight: bold;
            margin-bottom: 5px;
            color: #2c3e50;
        }
        
        .signature-content {
            display: flex;
            gap: 20px;
        }
        
        .qr-code {
            width: 60px;
            height: 60px;
        }
        
        .signature-details {
            flex: 1;
        }
        
        .signature-line {
            margin-bottom: 5px;
            font-size: 10px;
        }
        
        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 10px;
            color: #7f8c8d;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <div class="header">
            <div class="invoice-type-badge">{{invoiceType}}</div>
            <div class="invoice-title">INVOICE</div>
            <div class="invoice-number">Invoice #: {{invoiceNumber}}</div>
            <div class="invoice-date">Date: {{formatDate invoiceDate}}</div>
        </div>
        
        <div class="period-info">
            Period: {{formatDate startDate}} - {{formatDate endDate}}
        </div>
        
        <div class="details-section">
            <div class="vendor-details">
                <div class="section-title">VENDOR</div>
                <div class="detail-row">Name: {{vendor.name}}</div>
                <div class="detail-row">Code: {{vendor.code}}</div>
                <div class="detail-row">Contact: {{vendor.contactPerson}}</div>
                <div class="detail-row">GST: {{vendor.gstNumber}}</div>
            </div>
            
            <div class="plant-details">
                <div class="section-title">PLANT</div>
                <div class="detail-row">Name: {{plant.name}}</div>
                <div class="detail-row">Code: {{plant.code}}</div>
                <div class="detail-row">Address: {{plant.address}}</div>
            </div>
        </div>
        
        {{#if materialBreakdown}}
        <div class="material-breakdown">
            <div class="breakdown-title">MATERIAL BREAKDOWN</div>
            <table class="breakdown-table">
                <thead>
                    <tr>
                        <th>Material</th>
                        <th>Total Qty</th>
                        {{#if (eq invoiceType "purchase")}}
                        <th>Moisture</th>
                        <th>Dust</th>
                        {{/if}}
                        <th>Final Qty</th>
                        <th>Rate</th>
                        <th>Amount</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each materialBreakdown}}
                    <tr>
                        <td>{{materialName}}</td>
                        <td>{{totalQuantity}}</td>
                        {{#if (eq ../invoiceType "purchase")}}
                        <td>{{totalMoistureQuantity}}</td>
                        <td>{{totalDustQuantity}}</td>
                        {{/if}}
                        <td>{{finalQuantity}}</td>
                        <td>₹{{rate}}</td>
                        <td>₹{{totalAmount}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        {{/if}}
        
        {{#if paletteBreakdown}}
        <div class="material-breakdown">
            <div class="breakdown-title">PALETTE BREAKDOWN</div>
            <table class="breakdown-table">
                <thead>
                    <tr>
                        <th>Palette Type</th>
                        <th>Quantity (kg)</th>
                        <th>Rate (₹/kg)</th>
                        <th>Amount (₹)</th>
                    </tr>
                </thead>
                <tbody>
                    {{#if paletteBreakdown.looseQuantity}}
                    <tr>
                        <td>Loose Palette</td>
                        <td>{{paletteBreakdown.looseQuantity}}</td>
                        <td>₹{{paletteRates.loose}}</td>
                        <td>₹{{paletteBreakdown.looseAmount}}</td>
                    </tr>
                    {{/if}}
                    {{#if paletteBreakdown.packedQuantity}}
                    <tr>
                        <td>Packed Palette</td>
                        <td>{{paletteBreakdown.packedQuantity}}</td>
                        <td>₹{{paletteRates.packed}}</td>
                        <td>₹{{paletteBreakdown.packedAmount}}</td>
                    </tr>
                    {{/if}}
                </tbody>
            </table>
        </div>
        {{/if}}
        
        <div class="summary-section">
            <div class="summary-box">
                <div class="section-title">SUMMARY</div>
                <div class="detail-row">Total Quantity: {{totalQuantity}} kg</div>
                <div class="detail-row">Subtotal: ₹{{totalAmount}}</div>
            </div>
            
            <div class="gst-box">
                <div class="gst-title">GST BREAKDOWN</div>
                {{#if gstApplicable}}
                    {{#if gstType}}
                        {{#if (eq gstType "CGST_SGST")}}
                        <div class="gst-row">CGST ({{cgstRate}}%): ₹{{gstAmounts.cgst}}</div>
                        <div class="gst-row">SGST ({{sgstRate}}%): ₹{{gstAmounts.sgst}}</div>
                        {{else if (eq gstType "IGST")}}
                        <div class="gst-row">IGST ({{gstRate}}%): ₹{{gstAmounts.igst}}</div>
                        {{/if}}
                        <div class="gst-row" style="font-weight: bold; margin-top: 10px;">Total GST: ₹{{totalGST}}</div>
                    {{else}}
                    <div class="gst-row">No GST Applied</div>
                    {{/if}}
                {{else}}
                <div class="gst-row">No GST Applied</div>
                {{/if}}
            </div>
        </div>
        
        <div class="final-amount-box">
            <div class="final-amount-label">FINAL AMOUNT</div>
            <div class="final-amount-value">₹{{finalAmount}}</div>
        </div>
        
        <div class="signature-section">
            <div class="signature-title">DIGITAL SIGNATURE & VERIFICATION</div>
            <div class="signature-content">
                <div class="qr-code">
                    <img src="{{qrCodeDataUrl}}" alt="QR Code" style="width: 100%; height: 100%;">
                </div>
                <div class="signature-details">
                    <div class="signature-line">Digital Signature:</div>
                    <div class="signature-line">{{signature}}</div>
                    <div class="signature-line">Timestamp: {{timestamp}}</div>
                    <div class="signature-line">Authorized by: {{createdBy.name}}</div>
                    <div class="signature-line">Biofuel Management System</div>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <div>Generated by Biofuel Management System</div>
            <div>Generated on: {{formatDate generatedAt}}</div>
        </div>
    </div>
</body>
</html>
